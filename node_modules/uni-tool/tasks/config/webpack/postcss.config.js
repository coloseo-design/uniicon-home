const { getUniFile } = require('../../../utils/file');

const overrideBrowserslist = getUniFile().browserslist || [
  '>1%',
  'last 4 versions',
  'Firefox ESR',
  'not ie < 9',
];

module.exports = {
  plugins: {
    autoprefixer: {
      overrideBrowserslist,
      flexbox: 'no-2009',
    },
  },
};
